# Review Examples

This area holds example-backed review knowledge.

Examples are used as retrieval evidence and pattern support for the review
workflow. They are not authoritative rules on their own.

Use:

- `good/` for preferred patterns
- `bad/` for anti-patterns and failure modes
