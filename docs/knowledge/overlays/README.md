# Knowledge Overlays

Overlays provide composable slices of review knowledge that can be selected by
dimension.

## Dimensions

- `language/` for language or ecosystem overlays
- `repo/` for repository-family or environment overlays
- `topic/` for focused review domains such as testing, security, docs, or API

Overlays should refine or extend the baseline guides, not replace canonical
reference material.
