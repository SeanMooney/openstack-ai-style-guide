---
galaxy_info:
  author: Sean Mooney
  description: Extract Zuul context and analyze git commits for AI code review
  license: Apache-2.0
  min_ansible_version: "2.9"
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - ai
    - codereview
    - zuul
    - git
    - openstack

# Note: Role dependencies are not supported in Zuul's Ansible runtime.
# Role orchestration is handled by the playbook using include_role tasks.
# This role expects ai_review_setup to have been executed first.
dependencies: []
