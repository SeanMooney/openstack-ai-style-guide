---
# AI Context Extraction Role
# Extracts Zuul context and analyzes git commits

- name: Extract Zuul context
  ansible.builtin.include_tasks: extract-zuul-context.yaml

- name: Analyze git commit
  ansible.builtin.include_tasks: analyze-commit.yaml
