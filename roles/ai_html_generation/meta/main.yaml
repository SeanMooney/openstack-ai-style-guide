---
galaxy_info:
  author: Sean Mooney
  description: Generate enhanced HTML reports from JSON code review data
  license: Apache-2.0
  min_ansible_version: "2.9"
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - ai
    - codereview
    - html
    - reporting

# Note: Role dependencies are not supported in Zuul's Ansible runtime.
# Role orchestration is handled by the playbook using include_role tasks.
# This role expects ai_code_review to have been executed first.
dependencies: []
